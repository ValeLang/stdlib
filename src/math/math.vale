
func min(a int, b int) int {
  if (a <= b) { a } else { b }
}

func max(a int, b int) int {
  if (a >= b) { a } else { b }
}

func abs(a int) int {
  if (a < 0) { a * -1 } else { a }
}

func signum(a int) int {
  if (a < 0) { -1 }
  else if (a > 0) { 1 }
  else { 0 }
}
